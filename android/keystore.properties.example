# Copie para android/keystore.properties (gitignored) e preencha com as
# credenciais reais do upload keystore. Lido por app/build.gradle no signing
# de release. NUNCA versione o arquivo real com os valores preenchidos.
MOVT_UPLOAD_STORE_FILE=C:\\caminho\\para\\seu\\upload.jks
MOVT_UPLOAD_STORE_PASSWORD=
MOVT_UPLOAD_KEY_ALIAS=
MOVT_UPLOAD_KEY_PASSWORD=
